spring:
  datasource:
    url: jdbc:h2:mem:kg;MODE=PostgreSQL
    username: sa
    password: ""
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
management:
  endpoints:
    web:
      exposure:
        include: health

# Test-only value for the injected admin token (main config is fail-closed).
admin:
  token: test-admin-token
